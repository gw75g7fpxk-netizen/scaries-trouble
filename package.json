{
  "name": "scaries-trouble",
  "version": "1.0.0",
  "description": "A Phaser.js game featuring a movable character controlled via D-pad or keyboard",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "keywords": [
    "phaser",
    "game",
    "typescript"
  ],
  "license": "MIT",
  "dependencies": {
    "phaser": "^3.60.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^8.0.10"
  }
}
